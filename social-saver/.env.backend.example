# ─────────────────────────────────────────────────
# BACKEND SECRETS  →  Supabase Edge Functions runtime
# Never put these in a .env file. Set them via CLI:
#   npx supabase secrets set KEY=value
# They are stored encrypted by Supabase and injected
# into edge functions via Deno.env.get("KEY")
# ─────────────────────────────────────────────────

# ── Neo4j Aura ───────────────────────────────────
# Get from: console.neo4j.io → your instance → Connect
# URI format:  neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_aura_generated_password_here

# ── Gemini (already set) ─────────────────────────
GEMINI_API_KEY=AIza...

# ── Demo auth key (protect mutating endpoints) ───
# Generate: openssl rand -hex 16
DEMO_KEY=your_32char_random_secret_here

# ── Supabase (auto-provided in edge functions) ───
# These are injected automatically — you don't need to set them:
# SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_ANON_KEY
