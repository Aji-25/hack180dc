# ─────────────────────────────────────────────────
# FRONTEND ENV → social-saver/.env  (or .env.local)
# Only PUBLIC keys go here — Vite exposes VITE_* to the browser.
# Never put OpenAI, Twilio, Neo4j passwords, or service-role keys here.
# ─────────────────────────────────────────────────

# Supabase (public anon key — safe to expose)
VITE_SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...your_anon_key_here

# Edge function base URL (Supabase URL + /functions/v1)
VITE_EDGE_FUNCTION_URL=https://YOUR_PROJECT_REF.supabase.co/functions/v1

# Demo mode — the WhatsApp number used for the shared demo account.
# The dashboard reads ?u= from the URL; this is the fallback when no ?u= is present.
# Must match what the bot sends back after saving (the SHA-256 hash of the raw phone).
VITE_DEMO_PHONE=whatsapp:+14155238886

# ── Neo4j AuraDB (optional — enables Knowledge Graph view) ──────────────────
# Leave blank to use the local force-directed fallback graph.
VITE_NEO4J_URI=neo4j+s://xxxxxxxx.databases.neo4j.io
VITE_NEO4J_USER=neo4j
VITE_NEO4J_PASSWORD=your_neo4j_password_here
