# ─────────────────────────────────────────────────
# FRONTEND ENV  →  social-saver/.env.local
# Only PUBLIC keys go here (Vite exposes these to the browser).
# Never put Neo4j or Gemini credentials here.
# ─────────────────────────────────────────────────

# Supabase (public — safe to expose)
VITE_SUPABASE_URL=https://hxiwxeihlobarzphvvqi.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGci...

# Edge function base URL (same as Supabase URL + /functions/v1)
VITE_EDGE_FUNCTION_URL=https://hxiwxeihlobarzphvvqi.supabase.co/functions/v1

# Demo key — used by admin UI buttons (?admin=1) to call mutating endpoints
# This is the ONLY non-Supabase value the frontend ever sends
VITE_DEMO_KEY=your_32char_random_secret_here
